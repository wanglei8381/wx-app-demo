<import src="../../templates/head/head.wxml"/>

<view bindtap="onReportData">
  <template is="head" data="{{...header}}"/>
  <view>{{text}}</view>
  <view>{{nickName}}</view>
  <image src="{{avatarUrl}}"></image>
</view>
